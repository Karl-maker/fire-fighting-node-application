export enum DeviceType {
    VALVE = 'valve'
}

export enum DeviceStatus {
    ONLINE = 'online',
    OFFLINE = 'offline',
    ERROR = 'error',
    MAINTENANCE = 'maintenance',
    PENDING = 'pending'
}